plugins {
    id 'com.ldtteam.tableau.common' version '0.0.90'
}

tableau {
    project {
        group = "com.communi-suggestu.saecularia-caudices"
        publisher = "communi-suggestu"
        versioning {
            version = environmentVariable("VERSION")
                    .orElse(property("local.version"))
            suffix = ""
        }
    }
}
