plugins {
    id 'com.ldtteam.tableau.maven-publishing' version '0.0.74'
}

platform {
    minecraft {
        version = "1.21.11-pre2-20251124.022710"
    }
    project {
        library()
    }
}

tableau {
    project {
        group = "com.communi-suggestu.saecularia-caudices"
        publisher = "communi-suggestu"
        versioning {
            version = environmentVariable("VERSION")
                    .orElse(property("local.version"))
            suffix = ""
        }
    }
    maven {
        publishToLDTTeamMaven('communi-suggestu-saecularia-caudices')
        publishLocally()
        pom({
            usingMitLicense()
            usingGit()
        })
    }
}